import React from 'react'

type Props = {}

const Page = (props: Props) => {
    // WIP  Server action Onboard the user
    // WIP 200 || 201
    return (
        <div>Dashboard</div>
    )
}

export default Page